# NASA Project

Simple Node server that fetches NASA NEO feed.

Setup

```bash
npm install
```

Run

```bash
npm start
```

Notes
- Place your API key in `key.env` as `YOUR_API_KEY=your_key_here`.
- Node 18+ recommended for native fetch; `node-fetch` is included for compatibility.
